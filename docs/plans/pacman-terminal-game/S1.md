# S1 — Fix all clippy lints

## Write set
- `src/app.rs`
- `src/maze.rs`
- `src/entities.rs`
- `src/ghosts.rs`
- `src/scoring.rs`
- `src/scoreboard.rs`
- `src/levels.rs`
- `src/input.rs`
- `src/render.rs`

## Description
Fix all 93 clippy errors so `cargo clippy -- -D warnings` passes cleanly. All 66 tests must continue to pass.

## Fixes by file

### app.rs
1. Remove unused imports: `Tile`, `consume_fruit`, `is_wall`, `LevelConfig`, `Stdout`
2. Prefix unused `points` with `_` in match arms (lines 403, 407)
3. Replace `(upper >= 'A' && upper <= 'Z')` with `('A'..='Z').contains(&upper)`
4. Replace `(upper >= '0' && upper <= '9')` with `('0'..='9').contains(&upper)`
5. Replace `(state.power_pellet_timer / 2) % 2 == 0` with `(state.power_pellet_timer / 2).is_multiple_of(2)`
6. Remove `as u32` casts on `fright_ticks` (lines 411, 413) since it's already u32
7. Remove unused `show_high_scores: bool` field from `GameState` (line 49) and its initialization (line 80)
8. Remove `Action::Up` and `Action::Down` match arms (lines 182-187) since those variants are being removed from the Action enum

### maze.rs
9. Remove `Fruit(u32)` variant from `TileEffect` enum
10. Remove `consume_fruit` function

### entities.rs
11. Remove `Dying(u8)` and `Respawning` variants from `PacManState`
12. Remove `Scatter` variant from `GhostMode`
13. Remove `spawn` field from `Ghost` struct
14. Remove `Ghost::reset` method
15. Remove `None` variant from `CollisionEvent`
16. Remove `Dying` variant from `MoveResult`
17. Remove `wrap_direction` function
18. Collapse nested if at line 108

### ghosts.rs
19. Remove unused import `PacManState`
20. Remove `None` variant from `GhostEvent`
21. Remove `is_ghost_in_lair` function
22. Remove `mut` from `valid` variable in `random_direction`

### scoreboard.rs
23. Replace `entries.sort_by(|a, b| b.score.cmp(&a.score))` with `entries.sort_by_key(|b| std::cmp::Reverse(b.score))`

### input.rs
24. Remove `Up` and `Down` variants from `Action` enum

### render.rs
25. Remove unused import `Direction`
26. Replace `ratatui::widgets::Clear::default()` with `ratatui::widgets::Clear`
27. Reduce `render_game` argument count from 11 to ≤7 by creating a `RenderContext` struct or by grouping parameters. Group `(score, lives, level)` into a small tuple/bundle or pass `&GameState`.

### levels.rs
28. Remove unused fields from `LevelConfig`: `width`, `height`, `portal_pair_a`, `portal_pair_b`, `ghost_speed_factor`
29. Fix collapsible if at line 466
30. Fix manual range contains at line 178
31. Fix manual range contains at line 525
32. Fix all needless_range_loop lints (~46 occurrences). Replace `for x in range { tiles[y][x] = W; }` patterns with slice fills like `tiles[y][start..end].fill(W)` or `for cell in &mut tiles[y][start..end] { *cell = W; }`. For nested init loops `for y in 1..30 { for x in 1..30 { tiles[y][x] = D; } }` use `for row in &mut tiles[1..30] { row[1..30].fill(D); }`.

## Validation
After all fixes, run:
```
cargo clippy -- -D warnings
cargo test
```

Both must pass with zero errors.
